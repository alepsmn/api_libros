[project]
name = "api-libros"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alepsmn", email = "alepsmn@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.1",
]

[project.scripts]
api-libros = "api_libros:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
